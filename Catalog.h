#include "Record.h"

#include <set>

using namespace std;


class Catalog {
    private:
        set<Record> setOfRecords_;
    public:
        Catalog(set<Record> records) {

        };

        Record search(int key) {

        };
};