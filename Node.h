#include "Catalog.h"

#include <set>

using namespace std;

template <typename T>

class Node {
    private:
        Catalog catalog;
        T label;
    public:
        Node(T label, int[] catalog) {

        }

        Set<Node> getNeighbors() {

        }
};